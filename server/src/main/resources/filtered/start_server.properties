#
# properties for scripts/start_server.sh
#
# on a remote server, edit these as you please and re-run start_server.sh
#
# during the project build, these properties are relpcaed by the
# maven-assembly-plugin, and properties are in pom.xml
#

JAVA_GARBAGE_COLLECT_OPTIONS='${server.gcOptions}'
JAVA_MIN_HEAP_GB='${server.xmsGB}'
JAVA_MAX_HEAP_GB='${server.xmxGB}'

FABRIC_LAUNCHER_FILENAME='${url.fabric.launcherName}'
